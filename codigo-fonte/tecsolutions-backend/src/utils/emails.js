// src/utils/emails.js
// Placeholder para utilitários de e-mail (ex.: confirmação de proposta, relatório mensal).
export function sendEmailMock(to, subject, html) {
  // Somente mock para desenvolvimento inicial
  console.log('📧 [MOCK EMAIL]', { to, subject, htmlLength: html?.length ?? 0 });
}
